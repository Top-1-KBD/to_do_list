[tool.poetry]
name = "to-do-list"
version = "0.1.0"
description = ""
authors = ["Erwan"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pytest = "^7.4.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
